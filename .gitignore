# Ignore absolutely everything at the project root by default
/*

# Un-ignore the top-level things we need
!/Source
!/Config
!/*.uproject
!/Content
!/.gitignore

# Content is only partially wanted, so re-ignore everything inside it...
/Content/*
# ...then whitelist just the FPS subfolder
!/Content/FPS